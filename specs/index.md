Runtime Contract Index
======================

Purpose
-------

Use this index when changing HTTP behavior, payloads, or requirement traceability for a service runtime.

Artifacts
---------

- No HTTP contracts yet. Add `specs/<service>/openapi.yaml` when a service exposes HTTP.
